import React from 'react';

const TimelineContext = React.createContext({});
//Context passes start and end view times
// Also allows item stack heights to be shared from grandchild components

export default TimelineContext
